name: Publish Docker Image

on:
  push:
    branches: [ "*" ]
    tags:
      - "*"

jobs:

  docker:
    name: Publish - Docker Hub
    runs-on: ubuntu-22.04
    env:
      REPO: ${{ secrets.DOCKER_REPO }}

    steps:
      - uses: actions/checkout@v1
      - name: Login to Docker Hub
        run: docker login -u ${{ secrets.DOCKER_USER }} -p ${{ secrets.DOCKER_PASS }}
      - name: Build Docker image
        run: docker build -t fino-cars-service:latest -t fino-cars-service:${GITHUB_SHA::8} -t fino-cars-service:${GITHUB_REF#refs/*/} .
      - name: Publish Docker image
        run: docker push fino-cars-service --all-tags


#   Deployment works as manual by pulling the latest image on EC2 Instance

